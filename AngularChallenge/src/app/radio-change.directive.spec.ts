import { RadioChangeDirective } from './radio-change.directive';

describe('RadioChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioChangeDirective();
    expect(directive).toBeTruthy();
  });
});
